
public class Djikstra {
	//priority queue
	//hashmap for weight of nodes (starts as infinity) 
	//edges as distance
	//finds shortest path from starting point to end point where both points are known
}
